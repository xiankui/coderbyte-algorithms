/**
 * Dutch national flag sorting problem
 * @desc you know that the array is only included three number, such as 0, 1, 2, sorted it in O(n) times
 */

// flag is 0, 1, 2
let arr = [2,0,0,1,2,1];

function sorting (arr) {
  const low = 0,
        mid = 1,
        high = 2;

  for (let i = 0; i < arr.length; i++) {

  }
}